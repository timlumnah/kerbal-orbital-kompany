// Early unlocks for a few utility parts
// Put this anywhere under GameData (e.g., GameData/zzz_EarlyParts/EarlyParts.cfg)

// (1) EAS-4 Strut
@PART[strutConnector]:FINAL
{ @TechRequired = start }

// (2) Modular Girder Segment XL
@PART[trussPiece3x]:FINAL
{ @TechRequired = start }

// (3) SP-L 1x6 Photovoltaic Panels (include a few likely internal names to be safe)
@PART[solarPanels2]:FINAL            // common internal for SP-L 1x6
{ @TechRequired = start }
@PART[solarPanels3]:FINAL            // sometimes used for 1x6/2x3 variants
{ @TechRequired = start }
@PART[solarPanels1x6]:FINAL          // fallback name some packs use
{ @TechRequired = start }

// (4) PB-NUK Radioisotope Thermoelectric Generator
@PART[rtg]:FINAL
{ @TechRequired = start }

// (5) Z-1k Battery
@PART[batteryBankLarge]:FINAL
{ @TechRequired = start }

// (6) Z-4k Battery
@PART[ksp_r_largeBatteryPack]:FINAL
{ 	
	@TechRequired = start 
	@cost = 2000
}

// --- (7) Mobile Processing Lab MPL-LG-2
// @PART[Large_Crewed_Lab2]:FINAL { @TechRequired = start }   // fallback id seen in some installs
@PART[Large_Crewed_Lab]:FINAL
{ 
	@TechRequired = start
	@cost = 2000
}

// --- (7a) Science Jr.
@PART[science_module]:FINAL
{ 
	@TechRequired = start
}


// --- (8) SENTINEL Infrared Telescope
@PART[InfraredTelescope]:FINAL { @TechRequired = start }
@PART[SentinelTelescope]:FINAL { @TechRequired = start }    // fallback

// --- (9) Double-C Seismic Accelerometer
@PART[sensorSeismic]:FINAL { @TechRequired = start }

// --- (10) Atmospheric Fluid Spectro-Variometer
@PART[sensorAtmospheric]:FINAL { @TechRequired = start }    // fallback
@PART[sensorAtmosphere]:FINAL
{ 
	@TechRequired = start
	@cost = 2500
}

// --- (11) GRAVMAX Negative Gravioli Detector
@PART[sensorGravmax]:FINAL { @TechRequired = start }        // fallback
@PART[sensorGravimeter]:FINAL
{ 
	@TechRequired = start
	@cost = 1500
}

// --- (12) PresMat Barometer
@PART[sensorBarometer]:FINAL { @TechRequired = start }

// --- (13) “2HOT” Thermometer
@PART[sensorThermometer]:FINAL
{ 
	@TechRequired = start
	@cost = 10
}

// --- (14) EVA Experiments Kit (cargo)
@PART[evaExperimentKit]:FINAL { @TechRequired = start }
@PART[EVAExperimentKit]:FINAL { @TechRequired = start }     // fallback

// --- (15) EVA Repair Kit (cargo)
@PART[evaRepairKit]:FINAL { @TechRequired = start }
@PART[EVARepairKit]:FINAL { @TechRequired = start }         // fallback

// --- (16) SEQ-3c Conformal Storage Unit
@PART[conformalStorageUnit]:FINAL { @TechRequired = start }
@PART[SEQ-3C]:FINAL { @TechRequired = start }               // fallback

// --- (17) SEQ-9 Container Module
@PART[cargoContainer]:FINAL { @TechRequired = start }
@PART[SEQ-9]:FINAL { @TechRequired = start }                // fallback

// --- (18) SEQ-24 Cargo Storage Unit
@PART[cargoStorageUnit]:FINAL { @TechRequired = start }
@PART[SEQ-24]:FINAL { @TechRequired = start }               // fallback

// --- (19) Tail Fin
@PART[tailfin]:FINAL { @TechRequired = start }
@PART[winglet3]:FINAL { @TechRequired = start }             // some installs map tail fin here

// --- (20) Airbrakes — full patch with heat tweak is at entry (32) below; this duplicate removed
// Removed redundant @PART[airbrake1]:FINAL here. Entry (32) covers it
// with the heat resistance tweak and TechRequired = start already.

// --- (21) Aerodynamic Nose Cone
@PART[noseCone]:FINAL { @TechRequired = start }
@PART[standardNoseCone]:FINAL { @TechRequired = start }     // fallback

// --- (22) AE-FF1 Airstream Protective Shell (1.25m fairing)
@PART[fairingSize1]:FINAL { @TechRequired = start }
@PART[fairing_1_25m]:FINAL { @TechRequired = start }         // fallback

// --- (22b) AE-FF2 Airstream Protective Shell (2.5m fairing)
@PART[fairingSize2]:FINAL { @TechRequired = start }
@PART[fairing_2_5m]:FINAL { @TechRequired = start }         // fallback

// --- (23) AE-FF3 Airstream Protective Shell (3.75m fairing)
@PART[fairingSize3]:FINAL { @TechRequired = start }
@PART[fairing_3_75m]:FINAL { @TechRequired = start }        // fallback

// --- (24) TD-12 1.25m Decoupler
@PART[Decoupler_1]:FINAL { @TechRequired = start }          

// --- (24b) Mk2-R Parachute
@PART[parachuteRadial]:FINAL { @TechRequired = start }

// --- (25) Kelus-LV Bay Ladder
@PART[telescopicLadderBay]:FINAL { @TechRequired = start }

// --- (26a) LT-2 Landing Struts
@PART[landingLeg1-2]:FINAL { @TechRequired = start }

// --- (26b) LY-10 Small Landing Gear Wheel
@PART[SmallGearBay]:FINAL { @TechRequired = start }

// --- (27) Spotlight Mk1 | spotLight3
@PART[spotLight3]:FINAL { @TechRequired = start }

// --- (28) Heat Shield 2.5m
@PART[HeatShield2]:FINAL { @TechRequired = start }

// --- (29)Radial Decoupler Small
@PART[radialDecoupler]:FINAL { @TechRequired = start }

// --- (30a) Fuel Tank FL-T400
@PART[fuelTank]:FINAL {@TechRequired = start}

// --- (30b) Fuel Tank FL-T800
@PART[fuelTank_long]:FINAL {@TechRequired = start}

// --- (31) Fuel Line
@PART[fuelLine]:FINAL {@TechRequired = start}

// --- (32) Air brake
@PART[airbrake1]
{
  @maxTemp = 3300
  %skinMaxTemp = 3500
  @thermalMassModifier = 10
  @TechRequired = start
}

// --- (33) Inflatable Heat Shield
@PART[InflatableHeatShield]
{
  @TechRequired = start
  @MODULE[ModuleAnimateGeneric]
  {
    %isOneShot = false
    %disableAfterPlaying = false
    %allowManualControl = true
  }
}

